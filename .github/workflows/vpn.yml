name: Update VPN

on:
  push:
    branches: [ "vpn" ]
  pull_request:
    branches: [ "vpn" ]
  schedule:
  - cron: 0 2 * * 1-5

jobs:
  build:

    runs-on: ubuntu-latest
    container: 
      image: 444047485/kuipercourse_env:v1.0

    steps:
    - name: Update VPN
      run: bash vpn.sh
